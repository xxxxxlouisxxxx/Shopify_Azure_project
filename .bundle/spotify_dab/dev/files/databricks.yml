# This is a Databricks asset bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: 27614c17-90c6-49a8-bffe-2742e80e5f51


targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405619148768100.0.azuredatabricks.net
    presets:
      source_linked_deployment : false
  
  
  prod:
    mode: production
    workspace:
      host: https://adb-7405619148768100.0.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/louisxxxxo@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Prod/.bundle/${bundle.name}/${bundle.target}
    
    permissions:
      - user_name: louisxxxxo@gmail.com
        level: CAN_MANAGE
